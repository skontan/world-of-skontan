{
  "name": "world-of-skontan",
  "private": "true",
  "scripts": {
    "build": "yarn && yarn workspace @wos/server db:migrate && yarn workspace @wos/server db:push && yarn workspace @wos/server tsc",
    "start": "yarn workspace @wos/server start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.23.0",
    "jest": "^29.0.2",
    "typescript": "^4.8.2"
  }
}
